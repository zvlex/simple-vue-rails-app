.row
  %article.col-md-12
    %header
      %h1
        = @post.title
    %section
      = markdown @post.body

-# TODO: MOVE this post methods to serializer
%post-rating-buttons( post = "#{@post.to_json}")

%post-favorite-button( post-id = "#{@post.id}" :is-empty = "#{favorite_glyphicon_button(@post)}" quantity = "#{@post.favorites_quantity}" )

%hr
%comment-box( post-id = "#{@post.id}" )
